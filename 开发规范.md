# toNeko 开发规范

> 本文档根据代码仓库现状整理。**现状即规范**：以下条目全部来自既有代码的通行写法，新代码照此执行；与本文档不一致的旧代码以本文档为准逐步修正。

## 1. 项目概览

| 项 | 值 |
|---|---|
| 模组名 / MODID | toNeko（`org.cneko.toneko`） |
| MC 版本 | 1.21.1 |
| 架构 | Architectury 多平台：`common` + `fabric` + `neoforge`（`bukkit` 已停用，见 §3.4） |
| 映射 / Java | official Mojang Mappings，Java 21（`record`、text block、switch 表达式可用） |
| 前置 | GeckoLib 4.8.3（必装）；NeoForge 端另需 Forgified Fabric API（FFAPI） |
| 可选联动 | Trinkets（仅 Fabric）、LuckPerms、Patchouli、EMI、ModMenu |
| 协议 | GPL-3.0 |

## 2. 构建与运行

### 2.1 常用命令

```bash
# 编译验证（绕过 test 任务，见 16.5）
./gradlew :common:compileJava :fabric:compileJava :neoforge:compileJava

# 运行客户端
./gradlew :fabric:runClient     # 或 :neoforge:runClient

# 数据生成（仅 fabric，跑完不会自己退出，需手动停，见 16.6）
./gradlew :fabric:runDatagen

# 正式打包（CI 同款）
sh build.sh
```

### 2.2 版本管理

- 所有版本号集中在根 `gradle.properties`（`mod_version`、`minecraft_version`、各依赖 `xxx_version`）。
- 新增依赖：先在 `gradle.properties` 加版本变量，再在 build.gradle 用 `$rootProject.xxx_version` 引用，禁止散落硬编码。
- 升级 MC 版本时五件套同步：`minecraft_version` + `neoforge_version` + `fabric_api_version` + `forgified_fabric_api_version`（**FFAPI 必须与 fabric_api 版本配套**）+ 联动 mod 版本，并检查 `fabric.mod.json` 的 minecraft 依赖与 `neoforge.mods.toml` 的版本范围。
- 版本号经 `processResources` 的 `expand` 注入 mod metadata，无需手改。

### 2.3 产物

- 产物在 `fabric/build/libs/` 与 `neoforge/build/libs/`（`toneko-fabric-<版本>.jar` 等）。
- **不要把 jar 复制到 `result/` 目录**——`result/` 仅由 `build.sh`/CI 流程自己维护，日常构建后直接告知 `build/libs/` 路径即可。
- datagen 产物在 `fabric/src/main/generated/`（**提交进 git**）；NeoForge 侧对应 JSON（advancement/loot_table/loot_modifier/biome_modifier）需手工复制同步到 `neoforge/src/main/resources/data/`。

## 3. 总体架构决策（最高优先级，必须遵守）

### 3.1 common 只用 Fabric API（FFAPI 桥接）

- common 代码**直接 import Fabric API**（`networking.v1`、`command.v2`、`event.*`、`biome.v1`、`resource.v1`、`client.*` 等），NeoForge 端由 Forgified Fabric API 提供同包名桥接，因此网络、事件、命令等逻辑**只写一份在 common**，绝不写两份平台实现。
- 已在本项目验证可用的 FFAPI 模块：`networking.v1`、`client.networking.v1`、`command.v2`、`client.command.v2`、`item.v1`、`event.lifecycle.v1`、`client.event.lifecycle.v1`、`event.player`、`entity.event.v1`、`biome.v1`、`resource`、`message.v1`、`client.rendering.v1`、`client.keybinding.v1`、`object.builder.v1.trade`。
- 新增 fabric-api 依赖前先确认 FFAPI（0.116.7+2.2.1 对应 fabric-api 0.116.7）支持清单里有该模块；拿不准就拆到平台模块各自实现。
- **common 禁止使用 Fabric Loader 类**（唯一允许 `net.fabricmc.api.Environment/EnvType` 注解）；`PermissionUtil` 里 `Class.forName` 保护下的 `FabricLoader` 探测是例外而非范例。
- common 中命名带 `Fabric` 前缀的共享类（`FabricLanguageImpl`、`FabricSchedulerPoolImpl`）是历史遗留，新代码不要再这么命名。

### 3.2 双层注册模式

注册内容一律：**common 声明静态字段（初始 null）→ 平台模块写同名类真正注册并回填**。

- fabric：`Registry.register(BuiltInRegistries.X, toNekoLoc(id), 实例)`。
- neoforge：`DeferredRegister`（`ToNekoNeoForge` 持有全部 DR 字段并 `register(bus)`），回填通过 `reg()` 方法，且**必须放在 `FMLLoadCompleteEvent`**（注册表冻结前 `DeferredHolder.get()` 会抛错）。
- 例外（common 直接注册、两端共用）：数据组件 `ToNekoComponents`、结构处理器 `ToNekoStructures`、命令参数类型 `ModBootstrap`。
- 数据驱动注册表（附魔、唱片、伤害类型）只写 `ResourceKey` + `data/toneko/` JSON，完全不碰平台注册代码——新注册表优先考虑这条路。

### 3.3 初始化时序

平台入口（fabric `ToNeko.onInitialize` / neoforge `ToNekoNeoForge` 构造器）执行顺序固定：

```
Bootstrap.bootstrap() → ModBootstrap.bootstrap()
→ command → entities → biome → armor materials → blocks → items → 箱子战利品
→ effects → attributes → criteria → quirks → packets → recipes → menu types
```

- **需要 server 实例的初始化**（`ToNekoEvents.init()`、`ToNekoNetworkEvents.init()`、`PermissionUtil.init()`、`ModMeta.INSTANCE.setServer(...)`）一律放 `ServerLifecycleEvents.SERVER_STARTED`（fabric 端如此；neoforge 端 FFAPI 支持预注册、在构造器直接调）。
- 客户端入口最后统一调 `org.cneko.toneko.common.mod.client.ToNekoClient.init()`。
- 新增系统：common 写逻辑 + 两个平台入口各加一行 `ToNekoXxx.init()`，顺序遵循上表。

### 3.4 bukkit（已停用，注意保持可移植）

`bukkit/` 已从 `settings.gradle` 注释掉，不参与构建。它复用 common 的**纯逻辑**代码（`Bootstrap`、`LanguageUtil`、`NekoQuery`、quirks 等）。因此：common 中不绑定 MC 平台的代码不要耦合平台 API，保持可被降级端口复用。

## 4. 包结构

```
common/src/main/java/org/cneko/toneko/
├── common/
│   ├── Bootstrap.java / ModMeta.java     # 跨平台引导与全局状态
│   ├── api/          # 非 MC 绑定的纯逻辑层：Permissions、TickTasks、NekoQuery
│   ├── mod/          # mod 主体
│   │   ├── ModBootstrap.java             # 平台无关初始化总入口
│   │   ├── abilities/ blocks/ client/ codecs/ commands/ effects/ entities/
│   │   ├── events/    # 注册壳（转发 Fabric 回调）+ CommonXxx 业务类
│   │   ├── genetics/  impl/ items/ misc/ mixin/ packets/ quirks/ recipes/
│   │   ├── util/ worldgen/ api/          # mod/api：对外扩展点（注册表+接口）
│   │   └── entities/ai/goal/             # 实体 AI Goal
│   └── util/          # ConfigUtil、AIUtil、TTSUtil、HttpClient 等跨平台工具
fabric/src/main/java/org/cneko/toneko/fabric/    # 入口 + 平台 twin 类
neoforge/src/main/java/org/cneko/toneko/neoforge/
```

## 5. 命名规范

| 类别 | 惯例 | 示例 |
|---|---|---|
| 注册表容器类 | `ToNekoXxx`，common 声明 + 平台同名类 | `ToNekoBlocks`、`ToNekoItems` |
| 平台类后缀 | fabric 用 `Fabric`，neoforge 用 `Neo`/`NeoForge` | `ToNekoEffectFabric` / `ToNekoEffectNeoForge`、`ToNekoMenuTypesNeo` |
| 资源 ID | 一律经 `ResourceLocationUtil.toNekoLoc(...)`，snake_case，禁止裸写 | `toNekoLoc("legwear_workbench")` |
| 可注册类 ID | 类内 `public static final String ID` | `LegwearItem.Pantyhose40DItem.ID` |
| 实体类 | `NekoEntity` 抽象基类 + `<类型>NekoEntity` 变体 | `FightingNekoEntity`、`AdventurerNeko` |
| 实体类型 | 静态 `XXX_ID`（ResourceLocation）+ `XXX`（EntityType）字段对 | `ToNekoEntities.ADVENTURER_NEKO_ID` |
| 静态注册表 | `XxxRegistry`，全静态 Map + register/get 方法 | `GeneticsRegistry`、`QuirkRegister`、`NekoSkinRegistry` |
| EntityDataAccessor | 常量 `XXX_ID`，序列化器只用 STRING/INT/FLOAT | `SKIN_DATA_ID`、`NEKO_ENERGY_ID` |
| 属性 ID | `ResourceLocation.fromNamespaceAndPath(MODID, "neko.xxx")` | `neko.degree` |
| NBT key | PascalCase 字符串 | `"NekoLevelFactors"`、`"Genome"` |
| Mixin | `XxxMixin`；`@Unique` 成员与 mixin-interface 实现必须 `toneko$` 前缀 | `toneko$slowTick`、`toneko$isNeko` |
| @Inject 处理器 | 与目标方法同名或 `onXxx` 前缀 | `PlayerEntityMixin.tick`、`onRender` |
| AI Goal | `NekoXxxGoal`，放 `entities/ai/goal/` | `NekoFollowOwnerGoal` |
| 网络包 | `XxxPayload`（record），玩家交互类放 `packets/interactives/` | `LegwearAdjustPayload` |
| 修饰符 ID | `neko_level_xxx_modifier`；基因 `genetic_<locus>_<suffix>` | `genetic_speed_slot_0_modifier` |
| 怪癖 ID | 小写 snake_case 字符串 | `"caress"`、`"zako"` |
| 屏幕 | `<方块>Screen` ↔ `<方块>.<方块>Menu`；普通屏带静态 `open()` | `LegwearWorkbenchScreen` |
| 方块 | 自定义方块必须带 `public static final MapCodec<X> CODEC = simpleCodec(...)` 并覆写 `codec()` | `ShengDengBlock.CODEC` |

## 6. 注册规范

### 6.1 新增物品/方块的完整清单（以 `legwear_workbench` 为模板）

1. **common 声明**：`mod/blocks/ToNekoBlocks.java` + `mod/items/ToNekoItems.java` 各加静态字段。
2. **类本体**（common，有逻辑才建类，纯占位直接 `new Item(...)`）：方块类自带 CODEC；多变体用「抽象基类 + 静态嵌套子类」（参考 `NekoArmor`/`LegwearItem`/`CatnipItem`），不要为每个变体建顶层类。
3. **fabric 注册**：`fabric/items/ToNekoBlocks.java` + `ToNekoItems.java`（方块物品用 `new BlockItem(...)` 或 `new ItemNameBlockItem(...)`）。
4. **neoforge 注册**：`neoforge/items/` 同名类 + 两处 `reg()` 回填。
5. **物品组两处都要加**：fabric 的 `modifyEntriesEvent` 与 neoforge 的 `displayItems` + `buildContents`（曾因漏加导致物品组不一致）。
6. **lang × 5**（en_us/zh_cn/zh_tw/ja_jp/ko_kr，见 §12）。
7. **资源**：`blockstates/`、`models/block/`、`models/item/`、`textures/block/`（多面方块按 `textures/block/<名>/{top,side,bottom}.png`）。
8. **配方**：原版合成 `data/toneko/recipe/<id>.json`；机器配方 `data/toneko/recipe/<机器名>/<id>.json`。
9. **音效**（如涉及）：`ToNekoSoundEvents` 加常量（`SoundEvent.createVariableRangeEvent`，**不注册**，靠 `sounds.json` 对应 key）+ `assets/toneko/sounds.json` + `sounds/` 文件。
10. 可选：标签、进度（fabric datagen）、EMI（`ToNekoEmiPlugin`）、箱子战利品、客户端渲染层注册（fabric `BlockRenderLayerMap` / neoforge `RegisterNamedRenderTypesEvent`，cutout 方块两处都要写）。

### 6.2 数据组件（DataComponentType）

- 定义在 common `misc/ToNekoComponents.java`，`Registry.register(BuiltInRegistries.DATA_COMPONENT_TYPE, ...)`。
- **必须同时配 `.persistent(Codec)` 与 `.networkSynchronized(StreamCodec)`**——只配 persistent 的组件在 `ClientboundContainerSetSlotPacket` 回包时被静默丢弃，表现为"客户端点击后按钮弹回默认"（legwear 四组件踩过此坑）。
- Codec 集中在 `mod/codecs/`（如 `CountCodecs`）。
- 使用：构造时 `new Item.Properties().component(...)` 注入，读取 `stack.getOrDefault(...)`。

### 6.3 菜单与屏幕（无 BlockEntity 模式）

- GUI 方块**不使用 BlockEntity**：右键 `player.openMenu(new SimpleMenuProvider(...))`，容器 `new SimpleContainer(n)`，菜单类以静态嵌套类内嵌在方块类中（`<方块>.XxxMenu`，构造函数按 vanilla 签名 + 完整签名双写）。
- `MenuType` 声明在 common `recipes/ToNekoMenuTypes.java`，fabric `Registry.register`、neoforge **先给字段赋值再延迟到 `RegisterEvent` 注册**（保证 `MenuScreens.register` 时字段非 null）。
- 屏幕注册统一在 common `client/ToNekoClient.java` 的 `MenuScreens.register`。

### 6.4 配方

- `RecipeType` 与 `RecipeSerializer` **分开注册**。
- Serializer 以静态嵌套类内嵌，`MapCodec<X> CODEC`（`RecordCodecBuilder.mapCodec`）+ `StreamCodec<RegistryFriendlyByteBuf, X> STREAM_CODEC` 成对出现。
- 配套 `XxxInput implements RecipeInput` 与 `XxxRecipePattern` 数据类。

### 6.5 实体

- common `ToNekoEntities`：每个实体一对「`XXX_ID` + 可空 `XXX` 字段」+ `@ApiStatus.Internal` 的 `Supplier<EntityType<T>>` 构建方法（**显式 `sized()`/`clientTrackingRange()`/更新间隔**）。
- fabric 直接 `FabricEntityType.Builder.createMob(...)` 注册；neoforge `DeferredRegister` + `reg()` 回填。
- 群系生成收敛到 common 的 `registerBiomeSpawns(...)`（FFAPI `BiomeModifications.addSpawn`）；neoforge 的 `RegisterSpawnPlacementsEvent` 放平台模块。
- 属性：每个实体静态 `createXxxAttributes()`，自定义属性放 `misc/ToNekoAttributes.java`（`RangedAttribute(...).setSyncable(true)`，注册走 `@ExpectPlatform`，见 §7）。

### 6.6 其他注册表备忘

- 进度触发器：id 用 `MODID + "/" + name` 字符串拼接（**不是** `toNekoLoc`），neoforge 的 CRITERION_TRIGGERS 注册表 namespace 固定 `"minecraft"`。
- 声音事件：不注册，`createVariableRangeEvent` + `sounds.json`。
- 盔甲渲染器 `getRenderer()` 返回类型写 `Object`，避免服务端加载客户端类（代码注释：返回 Object 是因为否则服务器没法启动）。
- 可选 mod 探测：`tryClass("dev.emi.trinkets.api.Trinket")` 门控（参考 fabric `ToNekoItems`）。

## 7. 平台差异的三种处理方式

1. **@ExpectPlatform + `<pkg>.<platform>.XxxImpl`**（Architectury 惯例，impl 包名镜像 common 包名加平台后缀）。目前只有 `ToNekoAttributes` 用了；**新代码的平台差异优先按此收敛**。
2. **平台 twin 类**（现状主流）：fabric/neoforge 各写一个 `ToNekoXxx`，入口手工串联。命名后缀 `Fabric`/`Neo`/`NeoForge` 区分。
3. **FFAPI 直接写 common**（首选，见 §3.1）：能用 Fabric API 表达的差异直接写 common。

平台 hack（反射取 `LootTable.pools`、`tryClass` 探测、`kill -9` 杀 datagen 等）必须**写注释说明原因与失败行为**——这是本项目既有风格（参考 neoforge `ChestLootInjection`）。

## 8. 实体与数据规范

### 8.1 数据双层分工

- **持久化**：`addAdditionalSaveData`/`readAdditionalSaveData` NBT，key PascalCase。**load 必须与 save 的 key 严格对称**——`INeko` 的 NickName 读取曾写成 `setNickName(getNickName())`（恒等于自身），是 load 不对称的典型事故。
- **客户端同步**：`SynchedEntityData` + `EntityDataAccessor`，**只用 STRING/INT/FLOAT 序列化器，禁止在 entityData 放复杂对象**。列表类值用分隔符字符串 + 缓存；服务端 slowTick（20 tick）对易失值强制重写兜底。
- 玩家与实体共有的猫娘数据必须实现 `INeko`，复用 `saveNekoNBTData`/`loadNekoNBTData` 默认方法，**不得各写一套**。玩家侧经 `PlayerEntityMixin`（`toneko$` 前缀 @Unique 字段）注入原版存档；死亡重生转移补 `restoreFrom` 注入（`ServerPlayerEntityMixin`）。

### 8.2 AI 存储与线程

- 实体 UUID 不可作持久 key：AI 历史用 `neko.getAIStorageId()`（含迁移逻辑 `resolveStorageId`/`migrateNekoStorage`）。
- `Level.getEntities/getBlockState` 等只允许主线程；后台线程回调里的 MC 操作必须 `player.getServer().execute(...)`，`ServerPlayNetworking.send` 必须 try-catch 忽略玩家断线。

### 8.3 能力系统四选一（不新建能力注册表）

按优先级选型：① `INeko` 默认方法 → ② 自定义属性（`ToNekoAttributes`）→ ③ 等级因子（`api/NekoLevelFactor` + `NekoLevelRegistry`，注册放 `ModBootstrap.bootstrap()`）→ ④ `Quirk`。带 tick 的状态类能力参照 `ClimbWallHandler`：静态 ConcurrentHashMap + 服务端 tick 权威结算 + 客户端 payload 上报。

### 8.4 遗传系统

- `Locus`（record）+ `Allele`（类，dominance 语义 5 隐性/10 野生/15 半显性/20 显性）+ `GeneticsRegistry` 全静态注册 + 野生池权重；新基因在 `ModBootstrap` 或 `ToNekoXxx.init()` 注册。
- 表达入口 `expressTraits()`（**仅服务端**执行；先清旧 Modifier/AI/回调再叠加）；改基因后必须重新 `expressTraits()`，客户端可见效果经 SynchedEntityData 同步。
- AI 注入用 `Allele.addAIGoal`（自动装卸清理），属性用 `addAttributeModifier`（id 幂等去重）。
- 数据包驱动：`toneko_genetics/` 目录 JSON 三阶段加载，`/reload` 时 `clearDynamicData()` 后重载；API 文档见 `docs/genetics_api.md`（以代码为准）。

### 8.5 怪癖系统

- 实现 `ModQuirk` 接口并继承 `Quirk` 基类（id snake_case）；`ToNekoQuirks` 持静态单例常量 + `init()` 注册。
- 触发逻辑统一收敛到 `events/CommonPlayerInteractionEvent`，怪癖内不做交互入口判断；怪癖固定只影响玩家。

### 8.6 实体 AI（NekoBrain 仲裁，最重要的一条）

- **移动一律 `nekoBrain.submitMove(pos/entity, speed, BehaviorPriority, source)`，禁止直接 `navigation.moveTo`**——所有移动意图由 `NekoBrain` 统一仲裁（高优先级替换、同优先级防抖、同源去重、卡住检测）。
- Goal 放 `entities/ai/goal/`，命名 `NekoXxxGoal`，继承原版 `Goal`，`setFlags(EnumSet.of(Flag.MOVE))`；变体子类在 `super.registerGoals()` 基础上追加/移除。
- 需要绕过 goal 系统的复杂行为（如仇恨）可以在 tick 里直接驱动，但必须提供 protected 重写点给子类。

## 9. 客户端与渲染规范

### 9.1 GeckoLib 资源路径铁律

- **实体模型**：显式全路径 `toNekoLoc("geo/xxx/yyy.geo.json")`（`getModelResource`）、`textures/neko/<skin>.png`、`animations/...`，并配 `checkResource()` 探测 + 兜底回退 common 资源（参考 `NekoRenderer`/`NekoBossRenderer`）。
- **物品/盔甲**：一律 `DefaultedItemGeoModel<>(loc(MODID, "<分类>/<名>"))`，三件套自动落在 `geo/item/<分类>/`、`textures/item/<分类>/`、`animations/item/<分类>/`。**禁止向 assets 根目录放 geo/png**（现有 `assets/toneko/legwear.geo.json` 及 `legwear*.png` 是迁移遗留脏文件，待清理）。
- 动画文件：`format_version 1.8.0` + 结尾 `"geckolib_format_version": 2`；动画 key 命名空间 `misc.*`/`move.*`/`express.*`；`express.*` 动画必须进 common 动画文件（`NekoEntity` 的 `triggerableAnim` key 与之一一对应，服务端经 `NekoExpressAnimPayload` 广播驱动）。
- **盔甲骨骼硬性约定**：geo 里必须提供 `biped<Part>` 空骨骼 + `armor<Part>`（parent=biped）+ 部件骨骼挂在 `armor<Part>` 下；左右腿独立骨骼用 L/R 后缀（渲染器按后缀做双 pass/隐藏）。
- **左右腿 pivot 坑**：`bipedLeftLeg` 桩 pivot 写 `[+2,12,0]`、`bipedRightLeg` 写 `[-2,12,0]`（跟随原版 HumanoidModel 符号）。写反会导致左右腿镜像互换（`neko_armor.geo.json` 的猫爪模型就是反的，一直没被发现）。

### 9.2 渲染器

- 盔甲/丝袜物品走 `createGeoRenderer` + `GeoRenderProvider` **懒加载单例**（避免与其他 mod 不兼容）；子类实现 `getRenderer()` 返回 `Object`。
- 实体渲染器：`NekoRenderer<T> extends GeoEntityRenderer<T>`，内部静态类 `NekoModel`；fabric 用 `EntityRendererRegistry.register`，neoforge 用 `RegisterRenderers`。
- BEWLR（如 `ShengDengBewlr`）：fabric `BuiltinItemRendererRegistry` / neoforge `RegisterClientExtensionsEvent` + `IClientItemExtensions`，**两处都要写**；`ShengDengBewlr` 必须用 `Sheets.translucentItemSheet()` + `NEW_ENTITY` 顶点格式写满 6 元素（cutout 格式缺 OVERLAY/NORMAL 会崩）。
- 皮肤兜底：`checkResource()` 探测不存在就回退 `common` 资源，新皮肤必须配兜底链。
- EMI 插件 `@EmiEntrypoint` 注解版**只保留一份**（common/neoforge/fabric 现有三份存在双注册风险，收敛为一份）。

### 9.3 屏幕与 GUI

- 实体交互屏走 `NekoScreenRegistry`（key = 实体注册 id）+ `NekoScreenBuilder` builder 组合 + `clone()` 工厂；绑实体屏实现 `INekoScreen`。
- **GUI 贴图路径必须带 `.png` 后缀**（`SimpleTexture` 不自动补）；MC GUI 贴图惯例 256×256，512 大图需显式传 `BG_TEXTURE_SIZE`。
- 1.21.1 下 `AbstractContainerScreen.tick()` 是 final（用 `containerTick()`）；`AbstractSliderButton.value` 是 protected（外部重置需内部类 setter）。
- 普通屏带静态 `open()` 并持有 `lastScreen` 供返回；翻译 key 用 `screen.toneko.*`（按钮/标签优先 `gui.toneko.*`，新功能不要混用历史前缀）。

### 9.4 HUD / 按键 / 粒子

- HUD 入口统一 `HudRenderCallback.EVENT` 单回调；绘制前置守卫 `player == null`、`hideGui`；裸 `RenderSystem` 绘制后**必须恢复 RenderState**。
- 按键：`ToNekoKeyBindings` 静态 `X_KEY` 字段 + `KeyBindingHelper.registerKeyBinding`，消费统一在 `ClientTickEvent.processKeyInput` 用 `while (key.consumeClick())`，去向三种（命令 / `XScreen.open()` / `ClientPlayNetworking.send`）。
- **无自定义粒子**：直接用原版 `ParticleTypes`，不造轮子。
- 客户端本地配置走 `config/toneko_client.json` + `ConfigBuilder` 声明式模式，与服务端 `config/toneko.json` 分离。

## 10. 网络包规范

### 10.1 新增网络包五步法（顺序固定，缺一不可）

1. `packets/` 下建 `XxxPayload` record（实现原版 `CustomPacketPayload`，**不用** FabricPacket；不用 PacketDistributor）。
2. 静态 `ID = new CustomPacketPayload.Type<>(toNekoLoc("小写下划线路径"))` + `StreamCodec.composite(...)`（字段多时手写 `StreamCodec.of`）；JavaDoc 写清**方向与字段语义**。
3. `ToNekoPackets.init()` 按方向注册（`playS2C()`/`playC2S()`）；**双向包注册两次**。
4. 接收端：C2S 集中在 `ToNekoNetworkEvents`（`registerGlobalReceiver`，handler 方法引用）；S2C 集中在 `client/events/ClientNetworkEvents`（回调必须 `context.client().execute(...)` 切主线程）。
5. 发送：服务端 `ServerPlayNetworking.send(player, ...)`；客户端 `ClientPlayNetworking.send(...)`；"广播"手动遍历 `PlayerUtil.getPlayerList()` + `distanceToSqr` 过滤。

### 10.2 C2S 安全校验链（`onLegwearAdjust` 是教科书范例）

① 权限 `PermissionUtil.has` → ② 上下文状态校验（打开的 menu 类型 / 手持物品 instanceof）→ ③ 服务端权威 `Mth.clamp` 不可信输入 → ④ 幂等去重再写 → ⑤ 距离校验（交互类 >64 格不执行）→ ⑥ 防作弊双校验（如 GenomeData）。

### 10.3 状态回同步

服务端改容器/乘客等客户端可见状态后，显式补发原版同步包（`ClientboundContainerSetSlotPacket`、`ClientboundSetPassengersPacket`）。

## 11. AI 子系统规范

- **唯一入口 `common/util/AIUtil`**：普通 `sendMessage` / SSE 流式 `sendMessageStream`；内部线程池 100 daemon 线程、60s 超时、玩家级冷却、历史总结→裁剪→落盘（`FileStorageUtil.saveConversation`）；**绝不绕过 AIUtil 直连 provider**。
- 分层挂载新 AI 能力：`AIServiceProvider`（接口 + `AIServiceProviderRegistry` 静态注册，OpenAI 兼容的复用 `OpenAIProvider.applyCommon()`）→ `PromptFactory`（`PromptRegistry` `%key%` 替换 + `sanitize`）→ 动作 handler（`NekoActionExecutor.register`，`getGuideLine()` 拼进 prompt，解析失败静默不阻塞聊天）→ 触发/管理器（`NekoProactiveManager`/`NekoTalkManager`/`NekoTriggerManager`）。
- 动作在服务端主线程执行；"走到目标再表现"用 `scheduleWalkToArrive` 模式。
- 配置双轨制：扁平键 `ai.key` + 每 provider 独立 `ai.providers.<id>.*`，切换时 `saveProviderConfig`/`loadProviderConfig`；运行时改配置后 `config.save()`。
- 显示统一走 `Messaging.sendNekoChat` → `NekoChatDisplayPayload`（客户端按配置选聊天栏/气泡），不要自己拼网络包。
- TTS 仅 Player2（`TTSUtil`：端口动态发现 → `/v1/tts/voices`、`/v1/tts/stop` 打断、`/v1/tts/speak`）。

## 12. 命令与事件规范

### 12.1 命令

- 新命令 = 新 `XxxCommand` 类 + 静态 `init()` + 平台入口注册（`CommandRegistrationCallback.EVENT`，common 内使用，两端可用）。
- 权限字符串常量集中在 `common/api/Permissions.java`（`command.toneko.xxx` / `command.tonekoadmin.xxx`），**每个子命令**加 `.requires(source -> PermissionUtil.has(source, Permissions.COMMAND_XXX))`；`PermissionUtil` luckperms 优先，否则按前缀映射 op 等级（tonekoadmin→4，其余→0），`init()` 时注册全部权限。
- 自定义参数类型放 `commands/arguments/`，经 `ArgumentTypeRegistry.registerArgumentType(toNekoLoc(...), ...)`（`ModBootstrap`）；补全用 `XxxSuggestionProvider` 静态字段。
- 消息统一 `TextUtil.translatable` + `"command.xxx.yyy"` 翻译键；executes 返回 int（1 成功/0 失败）。
- 命令要开 GUI 时服务端执行后发 payload，不直接操作客户端。

### 12.2 事件

- `events/` 下 `ToNekoXxxEvents` 是**纯注册壳**（把 Fabric 回调转发给业务类），业务逻辑放 `CommonXxxEvent`。
- 跨模块/对外通知用 Fabric `EventFactory.createArrayBacked` 自定义事件，定义在自己包（`api/events/`），触发方 `invoker()`，监听方 `register`。
- NeoForge 特有事件（`AddReloadListenerEvent`、`FMLLoadCompleteEvent`、`RegisterSpawnPlacementsEvent`）只出现在 neoforge 模块。

## 13. Mixin 规范

- 一律放 common 的 `common/mod/mixin/`（客户端进 `mixin/client/` 子包），同步登记 `common/src/main/resources/toneko.mixins.json`（client 与 mixins 数组分开列）。
- 仅平台差异必须 mixin 时放平台模块 `mixins/` 子包 + 独立 `<platform>.toneko.mixins.json`，并在 fabric.mod.json 的 mixins 数组登记。
- `@Unique` 方法与 mixin-interface 实现**必须 `toneko$` 前缀**，避免与其他 mod 冲突；@Inject 处理器用目标同名或 `onXxx`。
- **`defaultRequire: 1` 已是全局配置**——改注入目标方法签名会导致启动失败；能用 Fabric 事件（FFAPI 桥接）就不写 mixin。
- Access Widener：`common/src/main/resources/toneko.accesswidener`（fabric 模块有同步拷贝），neoforge 靠 common 的 AW 经 loom 应用。

## 14. 本地化规范

- **5 种语言成对**：`en_us.json`、`zh_cn.json`（两者必须全量）、`zh_tw.json`、`ja_jp.json`、`ko_kr.json`。新增文本一次性同步 5 个文件，不得只更 en/zh。
- key 命名：小写 snake_case、点号分级 `域.toneko.对象.属性`；同类型多条用数字后缀（0 起）；配置项 `.title`/`.des` 成对；advancement 用 `.title`/`.description` 成对。
- key 按前缀字母序排列、域内聚类；**JSON 不支持注释，不依赖注释，靠自解释 key**。
- key 中的对象名与注册名、纹理目录名一致（`neko_aggregator` 同时是 block key、模型、纹理目录名）。
- 值内嵌 Minecraft 格式码（`§d` 等）与 `%s` 参数；多段台词用 `///` 做语义分隔（参考 Mouflet 台词）。
- 新增前缀优先 `gui.toneko.*`/`hint.toneko.*`/`hud.toneko.*`，避免与历史 `screen.toneko.*` 混用。

## 15. 资源规范

- 纹理：目录即域 `textures/{block,item,gui,mob_effect,neko}/`，文件名 = 注册 id（snake_case），物品模型 `models/item/<id>.json` 的 `layer0` 与之同名；多面方块按面分文件 `<方块>/{top,side,bottom}.png`；GUI 纹理 `textures/gui/<名>.png`；实体皮肤 `textures/neko/<skin>.png`。
- `sounds.json`：顶层 key 与 `SoundEvent` id 一致（点分）；`"name"` 不带扩展名、与 `sounds/<子路径>.ogg` 一一对应；音乐 `"stream": true`；多音色 = 数组 + 数字后缀（`biu0..5`）；音频格式 ogg 为主、wav 视素材而定。
- GeckoLib 资源路径见 §9.1。
- Blockbench 源文件（`.bbmodel`）放仓库根 `models/`，不提交进 assets；只有导出的 geo/animations/textures 进资源。
- datagen 产物（fabric）提交进 git，neoforge 侧手工同步。

## 16. 文档规范

- **文档中英成对**：新增/修改 docs 必须同步 `_en.md`；README 用 `[语言](链接)` 互链并加粗当前语言。
- 功能列表用 `- 🧬 **遗传学系统** — 描述` 格式（emoji + 粗体 + 破折号）；命令用代码块；参数表用 markdown 表格。
- 文档口径与代码事实必须一致（现存问题：AI.md 与 AI_en.md 服务商列表已漂移；配置文件名为 `config/toneko.json`，MC 百科正文却写 `toneko.yml`——写文档时核对代码）。
- 废弃代码按原包结构归档到 `docs/deprecated/<模块名>/`，保留包路径。

## 17. 提交规范

- 提交信息保留中文口语风格可以，但**必须包含信息量**（"更新喵！"这类纯卖萌提交不鼓励）；一个提交 = 一个功能主题，避免数万行混合提交。
- **禁止提交构建产物**：`*.class`、`build/`、`stash-dir/`、`*.tar.gz` 等（历史上出现过）。
- lang 文件改动**只动新增/修改条目，禁止整文件重排**（现有全量重排产生数千行无意义 diff）。
- 中英文档改动同步进同一个提交。

## 18. 已知坑与陷阱

1. **省凳模型悬浮**：省凳 12 个模型（red/blue × stack0~5）元素 y 从 10 起步（视觉悬浮），碰撞/座位按 `ShengDengBlock.CELL_HEIGHT_PX = {16,19,22,25,28,31}`。**修模型让凳子落地时必须同步改 `CELL_HEIGHT_PX` 为 {6,9,12,15,18,21}**（12 个模型文件 + 注释一起改），否则碰撞箱、座位高度、失衡机制与视觉脱节。
2. **MC 纹理路径必须带 `.png` 后缀**：`SimpleTexture.TextureImage.load` 不自动补后缀；GeckoLib 正常是因为它内部自己补。
3. **`DyeColor.getTextureDiffuseColor()` 返回 ARGB 负数**：1.21.1 带 0xFF alpha 位，用于比较/传输/渲染必须 `& 0xFFFFFF`，否则"点击色块显示 default、发负数被当成移除组件"。
4. **染色乘法混合要求白底**：`VertexConsumer.setColor` 是贴图像素 × 染料色乘法，贴图底色必须纯白；UV 布局注意勒痕环（cuff）不要与本体 UV 重叠（曾导致白丝偏灰偏黑）。
5. **GeckoLib 盔甲左右腿 pivot 符号**：见 §9.1。
6. **runDatagen 不会自己退出**：build.sh 用 sleep 60 + kill -9 的 hack，本地跑完需手动停。
7. **`:common:build` 的 test 任务在新 JDK 下报错**（"Could not create task ':common:test' ... Type T not present"）：Gradle 8.11.1 与新 JDK 的 report container 反射问题，与代码无关。验证编译用 `:common:compileJava :fabric:compileJava :neoforge:compileJava` 或 `-x test`。
8. **`:neoforge:compileJava` 有既有 fabric-api 注解错误**（fabric-block-view-api 的 `@UnknownNullability` 无法附加）：javac 会同时报所有错误，若输出里只有这一个错误，说明自己的 neoforge 代码类型检查已通过。
9. **IDE 运行配置勿用仓库内 `.idea/runConfigurations/`**：VM 参数写死作者本机路径，需用 loom 重新生成（`./gradlew :fabric:runClient` 或 IDEA loom 任务）。
10. **EMI 插件双注册风险**：`@EmiEntrypoint` 注解版只保留一份（见 §9.2）。
11. **neoforge 物品组两处注册**（`displayItems` + `buildContents`）：漏一处会导致物品组不一致（见 §6.1 第 5 条）。
12. **`INeko` 的 NickName load 不对称**是历史 bug 样板：load 必须从 nbt 取值，不能回写自身。
13. **legwear 资产由脚本生成**（`/tmp/legwear_gen/gen_legwear_assets.py`，未入 repo，1 格 = 8px、256×256，per-face UV）：改丝袜贴图需重跑脚本；裙装接入点走 `LegwearItem` 体系 + `ZettaiRyouiki.compute(袜口高度, 裙摆高度)`（只需实现 `getSkirtHemHeight` 输入侧）。

## 19. 新功能开发检查清单

- [ ] common 写业务逻辑（类本体 + `ToNekoXxx` 静态声明）
- [ ] fabric / neoforge 平台 twin 类注册 + `reg()` 回填（neoforge 放 `FMLLoadCompleteEvent`）
- [ ] 两个平台入口各加一行 `ToNekoXxx.init()`（顺序遵循 §3.3）
- [ ] 资源 ID 一律经 `toNekoLoc()`；可注册类自带 `public static final String ID`；方块自带 `MapCodec CODEC`
- [ ] lang 5 文件同步新增（en_us/zh_cn 必须全量）
- [ ] 模型/纹理/blockstates/sounds.json（路径即 id，GUI 纹理带 `.png` 后缀）
- [ ] 网络包按五步法（Payload → ToNekoPackets 注册 → 接收端 → 发送端），C2S 过安全校验链
- [ ] 物品组两处（fabric 一处 + neoforge `displayItems`/`buildContents` 两处）
- [ ] 数据组件同时配 `.persistent` + `.networkSynchronized`
- [ ] 服务端改状态后补发原版同步包（`ClientboundContainerSetSlotPacket` 等）
- [ ] 客户端渲染层（cutout 方块）fabric/neoforge 两处注册；渲染器注册两处
- [ ] 后台线程回调 `server.execute(...)` 包线程切换，send 带 try-catch
- [ ] 新增 mixin 登记 `toneko.mixins.json`；`@Unique` 成员 `toneko$` 前缀
- [ ] 涉及新依赖：gradle.properties 加版本变量 + 确认 FFAPI 支持
- [ ] 文档中英同步；README 功能列表更新
- [ ] 编译验证：`./gradlew :common:compileJava :fabric:compileJava :neoforge:compileJava`（跳过 test）
